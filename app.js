var AWS = require('aws-sdk');
var s3 = new AWS.S3();
var bucketName = '';
var keyName = '';
var object = '';

exports.handler = async (event, context) => {
    // Determine the winning choice
    // Rock loses to paper
    // Paper
  
    // 
};
